services:
  db:
    image: mysql:8.0
    command: --innodb-buffer-pool-size=128M --skip-name-resolve
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
    ports:
      # 호스트 3307 -> 컨테이너 3306. 로컬 MySQL이 3306을 점유한 경우 충돌 방지용.
      # 컨테이너 내부 통신(app -> db:3306)은 영향 없음.
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    mem_limit: 512m
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s

  app:
    build:
      context: .
    environment:
      SPRING_PROFILES_ACTIVE: dockerdb,common,security
      DB_NAME: ${DB_NAME}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    ports:
      - "8080:8080"
    mem_limit: 768m
    depends_on:
      db:
        condition: service_healthy

volumes:
  mysql-data:
