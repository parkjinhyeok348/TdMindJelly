# MySQL 컨테이너 + Spring 앱이 공유하는 DB 접속 정보
# 이 파일을 .env 로 복사한 뒤 값을 채우세요: cp .env.example .env
DB_NAME=mind_jelly
DB_USERNAME=mindjelly
DB_PASSWORD=changeme-app-password
DB_ROOT_PASSWORD=changeme-root-password
